[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "personal_growth_navigator"
version = "0.1.0"
description = "A personal growth navigator application"
readme = "README.md"
requires-python = ">=3.6"
license = {text = "MIT"}
dependencies = [
    "flask",
    "werkzeug",
    "python-dotenv",
]
